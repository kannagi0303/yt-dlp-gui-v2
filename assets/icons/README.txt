# IconKind SVG Export

Source: Icons.xaml.cs

- Total IconKind entries: 31
- Exported SVG files: 30
- Skipped entries without path data: None

## Notes
- Each SVG uses `viewBox="0 0 24 24"`.
- Each SVG path is exported from the `Description(...)` data in `IconKind`.
- Fill color is `currentColor`, so you can style it with CSS or SVG attributes.
